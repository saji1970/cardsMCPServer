// RBAC management tool definitions and handlers are registered via src/tools/registry.ts
// This file exists for structural consistency with the project layout.
//
// Tools provided (see registry.ts): manage_users, get_audit_log.

export { toolDefinitions, handleToolCall } from "./registry";
